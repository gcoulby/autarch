# Which Ollama model to pull and use. Smaller models run on CPU.
# mistral         — good quality, ~4 GB, runs on CPU
# llama3.2:3b    — fast, lightweight, ~2 GB
# phi3:mini      — very fast, ~2 GB
OLLAMA_MODEL=mistral
